\documentclass[12pt,a4paper]{article}

% ── Encoding and language ─────────────────────────────────────────────────────

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[main=english,ngerman]{babel}

% ── Typography ────────────────────────────────────────────────────────────────

\usepackage{microtype}
\usepackage{setspace}
\usepackage{parskip}

\onehalfspacing

% Gives LaTeX a little flexibility before producing overfull lines.
\setlength{\emergencystretch}{2em}

% ── Page layout ───────────────────────────────────────────────────────────────

\usepackage[
    a4paper,
    margin=2.5cm
]{geometry}

% ── Graphics and figures ──────────────────────────────────────────────────────

\usepackage{graphicx}
\usepackage{float}
\usepackage{tikz}

\usetikzlibrary{arrows.meta}

% ── Tables ────────────────────────────────────────────────────────────────────

\usepackage{array}
\usepackage{booktabs}
\usepackage{ragged2e}
\usepackage{tabularx}

\newcolumntype{Y}{>{\RaggedRight\arraybackslash}X}

% Optional fixed-width, ragged-right column type:
\newcolumntype{L}[1]{%
    >{\RaggedRight\arraybackslash}p{#1}%
}

% ── Quotations and bibliography ───────────────────────────────────────────────

\usepackage{csquotes}

\usepackage[
    backend=biber,
    style=authoryear,
    sorting=nyt,
    maxcitenames=2,
    maxbibnames=99
]{biblatex}

\addbibresource{references.bib}

% ── Header and footer ─────────────────────────────────────────────────────────

\usepackage{fancyhdr}

\pagestyle{fancy}
\fancyhf{}

\lhead{\small Krüger, Mautner}
\rhead{\small LLM Prompt Injection Defences}
\cfoot{\thepage}

\setlength{\headheight}{14.5pt}

% Avoid uppercase section names in headers if automatic marks are used later.
\renewcommand{\sectionmark}[1]{\markright{#1}}

% ── Hyperlinks and PDF metadata ───────────────────────────────────────────────
% Load hyperref late, after most other packages.

\usepackage[
    colorlinks=true,
    linkcolor=black,
    citecolor=black,
    urlcolor=blue,
    pdftitle={LLM Prompt Injection Defences},
    pdfauthor={Denis Krüger, Sebastian Mautner}
]{hyperref}

\urlstyle{same}

% ── Document ──────────────────────────────────────────────────────────────────

\begin{document}

\input{sections/00_titlepage}
\input{sections/00_abstract}

\clearpage
\tableofcontents

\clearpage
\input{sections/01_einleitung}
\input{sections/02_llms_und_prompting}
\input{sections/03_prompt_injection}
\input{sections/04_bedrohungsmodell}
\input{sections/05_struq}
\input{sections/06_limits_of_current_defences}
\input{sections/07_praxis_und_management}
\input{sections/08_fazit}

\clearpage
\printbibliography[heading=bibintoc]

\end{document}